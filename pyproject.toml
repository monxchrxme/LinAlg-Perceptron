[project]
name = "LinAlg_Lab2_Perceptron"
version = "0.1.0"
description = "Realization of single layer perceptron"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "matplotlib>=3.10.9",
    "numpy>=2.4.5",
    "scikit-learn>=1.8.0",
]
